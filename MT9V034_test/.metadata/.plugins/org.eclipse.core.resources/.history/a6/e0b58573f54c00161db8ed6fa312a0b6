################################################################################
# Automatically-generated file. Do not edit!
################################################################################

-include ../makefile.init

RM := rm -rf

# All of the sources participating in the build are defined here
-include sources.mk
-include subdir.mk
-include src/subdir.mk
-include objects.mk

ifneq ($(MAKECMDGOALS),clean)
ifneq ($(strip $(C_DEPS)),)
-include $(C_DEPS)
endif
ifneq ($(strip $(S_UPPER_DEPS)),)
-include $(S_UPPER_DEPS)
endif
endif

-include ../makefile.defs

# Add inputs and outputs from these tool invocations to the build variables 
ELFSIZE += \
fifo_reader.elf.size \

ELFCHECK += \
fifo_reader.elf.elfcheck \


# All Target
all: fifo_reader.elf secondary-outputs

# Tool invocations
fifo_reader.elf: $(OBJS) ../src/lscript.ld $(USER_OBJS)
	@echo Building target: $@
	@echo Invoking: MicroBlaze gcc linker
	mb-gcc -Wl,--no-relax -Wl,-T -Wl,../src/lscript.ld -L../../hello_world_bsp_0/microblaze_mcs/lib -mlittle-endian -mcpu=v8.30.a -mxl-soft-mul -o"fifo_reader.elf" $(OBJS) $(USER_OBJS) $(LIBS)
	@echo Finished building target: $@
	@echo ' '

fifo_reader.elf.size: fifo_reader.elf
	@echo Invoking: MicroBlaze Print Size
	mb-size fifo_reader.elf  |tee "fifo_reader.elf.size"
	@echo Finished building: $@
	@echo ' '

fifo_reader.elf.elfcheck: fifo_reader.elf
	@echo Invoking: Xilinx ELF Check
	elfcheck fifo_reader.elf -hw ../../hw_platform_0/system.xml -pe microblaze_mcs  |tee "fifo_reader.elf.elfcheck"
	@echo Finished building: $@
	@echo ' '

# Other Targets
clean:
	-$(RM) $(OBJS)$(C_DEPS)$(EXECUTABLES)$(ELFSIZE)$(ELFCHECK)$(S_UPPER_DEPS) fifo_reader.elf
	-@echo ' '

secondary-outputs: $(ELFSIZE) $(ELFCHECK)

.PHONY: all clean dependents
.SECONDARY:

-include ../makefile.targets
